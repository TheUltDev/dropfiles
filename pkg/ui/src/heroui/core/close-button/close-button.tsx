import type {CloseButtonProps} from './types';

import {CloseButton as NativeCloseButton} from 'heroui-native';

export const CloseButton = NativeCloseButton;
export type {CloseButtonProps} from './types';
