import type {FieldErrorProps} from './types';

import {FieldError as NativeFieldError} from 'heroui-native';

export const FieldError = NativeFieldError;
export type {FieldErrorProps} from './types';
