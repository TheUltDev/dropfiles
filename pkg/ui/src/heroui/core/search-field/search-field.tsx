import type {SearchFieldProps} from './types';

import {SearchField as NativeSearchField} from 'heroui-native';

export const SearchField = NativeSearchField;
export type {SearchFieldProps} from './types';
