import type {ControlFieldProps} from './types';

import {ControlField as NativeControlField} from 'heroui-native';

export const ControlField = NativeControlField;

export {useControlField} from 'heroui-native';

export type {ControlFieldProps} from './types';
